# MicroLab MIC WebApp - Release v5 (GitHub complete package)

## Fix inclusi
- Aggiunta icona `.ico` come `favicon.ico` in:
  - root repo
  - `docs/`
  - `frontend/`
- Aggiornati `docs/index.html` e `frontend/index.html` con i link favicon.
- Aggiunto `index.html` in root con redirect automatico a `./docs/` (utile se Pages è impostato su root).
- Inclusa copia dell'icona come `microlab-mic-icon.ico`.

## Note deploy
- Per GitHub Pages usare preferibilmente:
  - **Source**: Deploy from a branch
  - **Branch**: `main`
  - **Folder**: `/docs`
